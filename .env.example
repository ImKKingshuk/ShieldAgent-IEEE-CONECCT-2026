# Optional provider keys for LLM-backed validation runs.
# Leave these empty for deterministic tests and non-LLM benchmark runs.
OPENROUTER_API_KEY=
CEREBRAS_API_KEY=
GOOGLE_AI_API_KEY=

# Default provider used when an LLM-backed command is requested.
SHIELDAGENT_LLM_PROVIDER=openrouter
